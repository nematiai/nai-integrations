# Django
SECRET_KEY=change-me
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8010,http://localhost:8012,http://127.0.0.1:8010,http://127.0.0.1:8012

# Database — use sqlite3 for local dev without Docker
# DB_ENGINE=django.db.backends.sqlite3
DB_ENGINE=django.db.backends.postgresql
DB_NAME=nemi
DB_USER=nemi
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Redis / Celery
REDIS_URL=redis://localhost:6379/0

# Token encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
TOKEN_ENCRYPTION_KEY=

# Storage providers (existing from nai-integrations)
BOX_CLIENT_ID=
BOX_CLIENT_SECRET=
DROPBOX_CLIENT_ID=
DROPBOX_CLIENT_SECRET=
GOOGLE_OAUTH2_CLIENT_ID=
GOOGLE_OAUTH2_CLIENT_SECRET=
ONEDRIVE_CLIENT_ID=
ONEDRIVE_CLIENT_SECRET=
